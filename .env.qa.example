# ============================================
# E2 — QA / Staging (Vercel Preview on `qa` branch)
# ============================================
# Set these in Vercel Dashboard → Settings → Environment Variables
# Scope: Preview, filtered to branch = qa

# Supabase (QA project — ideally separate from dev)
NEXT_PUBLIC_SUPABASE_URL=https://your-qa-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-qa-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-qa-service-role-key

# Application
NEXT_PUBLIC_APP_URL=https://qa.yojak.ai

# Auth — auto-confirm for easier QA testing
AUTH_AUTO_CONFIRM=true
