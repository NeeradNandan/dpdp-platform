#!/usr/bin/env sh

# Strip any Co-authored-by lines mentioning Cursor/cursoragent
if [ -f "$1" ]; then
  sed -i.bak '/^Co-authored-by:.*[Cc]ursor/d' "$1"
  sed -i.bak '/^Co-authored-by:.*cursoragent/d' "$1"
  rm -f "$1.bak"
fi
