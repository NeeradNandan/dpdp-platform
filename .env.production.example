# ============================================
# E3 — Production (Vercel Production on `main` branch)
# ============================================
# Set these in Vercel Dashboard → Settings → Environment Variables
# Scope: Production

# Supabase (production project — dedicated, separate instance)
NEXT_PUBLIC_SUPABASE_URL=https://your-prod-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-prod-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-prod-service-role-key

# Application
NEXT_PUBLIC_APP_URL=https://yojak.ai

# Auth — require email confirmation in production
AUTH_AUTO_CONFIRM=false
