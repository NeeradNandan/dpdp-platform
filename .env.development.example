# ============================================
# E1 — Development (Vercel Preview on `develop` branch)
# ============================================
# Set these in Vercel Dashboard → Settings → Environment Variables
# Scope: Preview, filtered to branch = develop

# Supabase (shared dev project)
NEXT_PUBLIC_SUPABASE_URL=https://your-dev-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-dev-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-dev-service-role-key

# Application
NEXT_PUBLIC_APP_URL=https://dev.yojak.ai

# Auth — auto-confirm users in dev
AUTH_AUTO_CONFIRM=true
